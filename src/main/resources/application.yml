app:
    timezone: Asia/Shanghai

spring:
    profiles:
        active: dev
    jackson:
        time-zone: ${app.timezone}
    rabbitmq:
        publisher-confirm-type: correlated
        publisher-returns: true
        template:
            mandatory: true
server:
    port: 25001
logging:
    charset:
        console: UTF-8
        file: UTF-8
jasypt:
    encryptor:
        password: prwq0421
        algorithm: PBEWITHMD5ANDDES
        bean: jasyptComputer
# Token配置
# Token私钥
token:
    privateKey: ENC(uJcujptsJM2XISNPFiTEGjUKS08qLtBN)
    # Access Token过期时间（毫秒）
    youngToken: 1000000
    # Refresh Token过期时间（毫秒）
    oldToken: 3000000000

wechat:
    pay:
        appId: wx_mock_app_id
        mchId: mock_mch_id
        notifyUrl: https://example.com/pay/wechat/callback
        apiV3Key: mock_api_v3_key
        privateKeyPath: classpath:cert/apiclient_key.pem
        merchantSerialNumber: mock_serial_number
        autoMockSuccess: false
